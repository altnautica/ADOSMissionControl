RTSP_URL_PATTERN=rtsp://host.docker.internal:8554/{deviceId}
PORT=3001
# Shared secret for viewer tokens (openssl rand -hex 32). Required: the relay
# refuses to start without it. Set the same value as VIDEO_RELAY_SECRET on Convex.
VIDEO_RELAY_SECRET=
CLOUDFLARE_TUNNEL_TOKEN=your-tunnel-token-here
